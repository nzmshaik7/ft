<%= form_for(@region) do |f| %>
  <% if @region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@region.errors.count, "error") %> prohibited this region from being saved:</h2>

      <ul>
      <% @region.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><font color="#800000">*</font><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :state_id, 'State' %><font color="#800000">*</font><br />
    <%= select("region", "state_id", @stateCollect, {prompt: 'Select State'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
