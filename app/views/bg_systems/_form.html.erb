<%= form_for(@bg_system) do |f| %>
  <% if @bg_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bg_system.errors.count, "error") %> prohibited this bg_system from being saved:</h2>

      <ul>
      <% @bg_system.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
