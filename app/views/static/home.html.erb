<center>
<h2>Futuretech 1.0 Home Page</h2>
<img src="/assets/logoanim/ffout006.jpg" border="0" name="mobi">
</center>

<% cu = current_user %>
<% if cu.nil? %>
  <font size="+1"><B>
  Please sign in from <a href="/users/sign_in"> the sign in page.</a>
  </B></font>
<% else %>
  Welcome <%= cu.first_name %> <%= cu.last_name %>. <br>
  <% if not cu.role.nil? %>
    You are classified as <%= cu.roleText %>. <br>
  <% end %>

  <% if cu.role == User::ROLE_ADMIN %>
    <a href="/users">Manage Users</a><br>
    <a href="/top/db">Manage Database</a><br>
    <a href="/top/creds">Credentials</a><br>
  <% end %>
  <% if cu.role == User::ROLE_ADMIN or cu.role == User::ROLE_MOCKER %>
    <a href="/mockup/internal_veh">View Mockup</a><br>
  <% end %>
  <% if cu.role == User::ROLE_ADMIN or cu.role == User::ROLE_DATAENT %>
    <a href="/top/gf">Ground Floor Service Visits</a><br>
  <% end %>
  <% if cu.role == User::ROLE_CUSTOMER %>
    (Customer section isn't ready yet.)<br>
  <% end %>
<% end %>

<!-- Find me in app/views/static/home.html.erb -->
