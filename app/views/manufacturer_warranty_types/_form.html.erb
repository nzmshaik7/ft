<%= form_for(@manufacturer_warranty_type) do |f| %>
  <% if @manufacturer_warranty_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manufacturer_warranty_type.errors.count, "error") %> prohibited this manufacturer_warranty_type from being saved:</h2>

      <ul>
      <% @manufacturer_warranty_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
