<%= form_for(@part_name) do |f| %>
  <% if @part_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_name.errors.count, "error") %> prohibited this part_name from being saved:</h2>

      <ul>
      <% @part_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if defined?(isGroundFloor) and isGroundFloor %>
    <input name="is_ground_floor" type="hidden" value="Y" />
  <% end %>
  <div class="field">
    <%= f.label :name %>:<font color="#800000">*</font>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
