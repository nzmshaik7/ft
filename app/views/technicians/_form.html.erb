<%= form_for(@technician) do |f| %>
  <% if @technician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@technician.errors.count, "error") %> prohibited this technician from being saved:</h2>

      <ul>
      <% @technician.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if defined?(isGroundFloor) and isGroundFloor %>
    <input name="is_ground_floor" type="hidden" value="Y" />
  <% end %>
  <div class="field">
    <%= f.label :employee_id %>:<font color="#800000">*</font>
    <%= select("technician", "employee_id", @employeeCollect,
                                               {prompt: 'Select Employee'}) %>
    &nbsp; &nbsp; &nbsp; &nbsp; 
    <%= f.label :level %>:<font color="#800000">*</font>
    <%= f.number_field :level, :size => 4 %>
    &nbsp; &nbsp; &nbsp; &nbsp; 
    <%= f.label :hourly_rate %>:<font color="#800000">*</font>
    $<%= f.text_field :hourly_rate, :size => 8 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
