<h1>Listing p</h1>




<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @ps.each do |p| %>
  <tr>
    <td><%= p.name %></td>
    <td><%= link_to 'Edit', edit_p_path(p) %></td>
    <td><%= link_to 'Destroy', p, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />


<%= link_to 'New P', new_p_path %>
&nbsp; | &nbsp; <a href="/top/db">All DB tables</a>
