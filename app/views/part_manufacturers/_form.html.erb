<%= form_for(@part_manufacturer) do |f| %>
  <% if @part_manufacturer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_manufacturer.errors.count, "error") %> prohibited this part_manufacturer from being saved:</h2>

      <ul>
      <% @part_manufacturer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
