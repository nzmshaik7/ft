<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><font color="#800000">*</font><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><font color="#800000">*</font><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :ssn %><br />
    <%= f.text_field :ssn %>
  </div>
  <div class="field">
    <%= f.label :store_id %><font color="#800000">*</font><br />
    <%= select("employee", "store_id", @storeCollect,
                                                  {prompt: 'Select Store'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
