<%= form_for(@purchase_order) do |f| %>
  <% if @purchase_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_order.errors.count, "error") %> prohibited this purchase_order from being saved:</h2>

      <ul>
      <% @purchase_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :po_number %><font color="#800000">*</font><br />
    <%= f.text_field :po_number %>
  </div>
  <div class="field">
    <%= f.label :vendor_id, 'Vendor' %><font color="#800000">*</font><br />
    <%= select("purchase_order", "vendor_id", @vendorCollect,
                                              {prompt: 'Select Vendor'}) %>
  </div>
  <div class="field">
    <%= f.label :date_time, 'Time ordered' %><font color="#800000">*</font>
    <br />
    <%= f.datetime_select :date_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
