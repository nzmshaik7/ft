<h2>Listing scheduled_service_items</h2>

<P>
Items that need to be serviced during a particular ScheduledService
for a particular vehicle.
</P>

<table border="1" class="ft_dbtab">
  <tr bgcolor="#F0FFF0">
    <th>Scheduled service</th>
    <th>Service description</th>
    <th>Other</th>
    <th colspan="3"><i>Actions</i></th>
  </tr>

<% @scheduled_service_items.each do |scheduled_service_item| %>
  <tr>
    <td>
      ID <%= scheduled_service_item.scheduled_service_id %>:
      <%= scheduled_service_item.scheduled_service.serviceText %>
    </td>
    <td><%= scheduled_service_item.service_description.name %></td>
    <td><%= scheduled_service_item.other %></td>
    <td><%= link_to 'Show', scheduled_service_item %></td>
    <td><%= link_to 'Edit', edit_scheduled_service_item_path(scheduled_service_item) %></td>
    <td><%= link_to 'Destroy', scheduled_service_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Scheduled service item', new_scheduled_service_item_path %>
&nbsp; | &nbsp; <a href="/top/db">All DB tables</a>
